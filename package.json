{
  "name": "polymarket-reverse-bot",
  "version": "1.0.0",
  "description": "Polymarket 15-minute Up/Down reverse arbitrage bot",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/*.test.js"
  },
  "dependencies": {
    "@polymarket/clob-client": "^5.8.1",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.2"
  }
}
